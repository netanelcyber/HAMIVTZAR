# Real deployment data and generated reports/write-ups are sensitive --
# never commit them. Allowlist just the durable docs (scope.md, README.md);
# everything else markdown/json in this directory is treated as a
# potentially sensitive, per-run artifact and stays local.
*.json
*.md
!scope.md
!README.md
__pycache__/
*.pyc
